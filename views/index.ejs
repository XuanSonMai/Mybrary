

<h2 class="page-header">Recently Girls</h2>
<div>

    <% books.forEach(book => { %>
        <a href="/book/<%= book.id%>">
           <img height="150" width="100" src="<%= book.coverImagePath %>">
        </a>
    <% }) %>


</div>
